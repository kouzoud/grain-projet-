# ==========================================
# Configuration Render.com - Link2Act
# ==========================================

services:
  # ==========================================
  # Base de données PostgreSQL + PostGIS
  # ==========================================
  - type: pserv
    name: Link2Act-db
    env: docker
    plan: free
    region: frankfurt
    dockerfilePath: ./postgres/Dockerfile.render
    envVars:
      - key: POSTGRES_DB
        value: Link2Act
      - key: POSTGRES_USER
        value: postgres
      - key: POSTGRES_PASSWORD
        generateValue: true
    disk:
      name: postgres-data
      mountPath: /var/lib/postgresql/data
      sizeGB: 1

  # ==========================================
  # Backend Spring Boot
  # ==========================================
  - type: web
    name: Link2Act-backend
    env: docker
    plan: starter
    region: frankfurt
    dockerfilePath: ./back/Dockerfile
    healthCheckPath: /actuator/health
    envVars:
      - key: SPRING_DATASOURCE_URL
        fromDatabase:
          name: Link2Act-db
          property: connectionString
      - key: SPRING_DATASOURCE_USERNAME
        fromDatabase:
          name: Link2Act-db
          property: user
      - key: SPRING_DATASOURCE_PASSWORD
        fromDatabase:
          name: Link2Act-db
          property: password
      - key: SPRING_JPA_HIBERNATE_DDL_AUTO
        value: update
      - key: SERVER_PORT
        value: 8080
      - key: JAVA_OPTS
        value: "-Xmx512m -Xms256m"
      - key: APPLICATION_SECURITY_JWT_SECRET_KEY
        generateValue: true
      - key: APPLICATION_SECURITY_JWT_EXPIRATION
        value: "86400000"
      - key: SPRING_MAIL_USERNAME
        sync: false
      - key: SPRING_MAIL_PASSWORD
        sync: false
      - key: APP_ADMIN_EMAIL
        value: admin1@Link2Act.com
      - key: APP_ADMIN_PASSWORD
        generateValue: true

  # ==========================================
  # Frontend React
  # ==========================================
  - type: web
    name: Link2Act-frontend
    env: docker
    plan: free
    region: frankfurt
    dockerfilePath: ./frontend/Dockerfile
    envVars:
      - key: VITE_API_URL
        fromService:
          name: Link2Act-backend
          type: web
          property: host
